---
id: mcp-server
title: ConfigCat MCP Server
description: Use the ConfigCat Model Context Protocol (MCP) server to manage feature flags & configs via the Public Management API directly from AI tooling.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

## Overview

The ConfigCat Model Context Protocol (MCP) server exposes the full power of the [Public Management API](/api/reference/configcat-public-management-api/) to AI assistants and MCP‑capable developer tools. It lets you Create, Read, Update and Delete entities (Products, Configs, Environments, Feature Flags, Segments, Tags, Webhooks, Integrations, etc.) safely and consistently.

> Management only. For evaluating feature flag values inside applications use the [SDKs](/sdk-reference/overview/) or the [ConfigCat Proxy](/advanced/proxy/proxy-overview/).

## Features

- Complete CRUD coverage of ConfigCat management entities
- Both Config (v1) and Config V2 feature flag/value endpoints
- Fine‑grained membership, permission group & invitation management
- Tagging, stale flag reporting, audit logs & code reference visibility
- Respect for API rate limits (propagates error details back to the client)

## Setup

Install & run via `npx` (no local clone required). Supply your Public Management API basic auth credentials (find them at https://app.configcat.com/my-account/public-api-credentials).

### Environment Variables

| Variable | Required | Default | Description |
| -------- | -------- | ------- | ----------- |
| `CONFIGCAT_API_USER` | ✔ | – | Management API basic auth username. |
| `CONFIGCAT_API_PASS` | ✔ | – | Management API basic auth password. |
| `CONFIGCAT_BASE_URL` |  | `https://api.configcat.com` | Override API host (rarely needed). |

Keep credentials secret. Prefer using your tool's secret storage facilities over committing them.

## Client Configuration Examples

Below are quick recipes for popular MCP clients.

<Tabs>
  <TabItem value="cursor" label="Cursor" default>

1. Open Preferences → Cursor Settings → MCP & Integrations
2. Click **Add Custom MCP**
3. Add (or merge) the snippet below into your JSON settings:

```json
{
  "mcpServers": {
    "configcat": {
      "command": "npx",
      "args": ["-y", "@configcat/mcp-server"],
      "env": {
        "CONFIGCAT_API_USER": "YOUR_API_USER",
        "CONFIGCAT_API_PASS": "YOUR_API_PASSWORD"
      }
    }
  }
}
```
4. Save – the server will start on demand.

  </TabItem>
  <TabItem value="claude" label="Claude Desktop">

1. Open **Settings** → **Developer**
2. Click **Edit Config**
3. In `claude_desktop_config.json` add:

```json
{
  "mcpServers": {
    "configcat": {
      "command": "npx",
      "args": ["-y", "@configcat/mcp-server"],
      "env": {
        "CONFIGCAT_API_USER": "YOUR_API_USER",
        "CONFIGCAT_API_PASS": "YOUR_API_PASSWORD"
      }
    }
  }
}
```
4. Save & restart Claude Desktop.

  </TabItem>
</Tabs>

## Available Tools

### Membership Management

#### Organizations
- `list-organizations` – List all organizations

#### Members
- `list-organization-members` – List organization members
- `list-pending-invitations` – List pending invitations (product scope)
- `list-pending-invitations-org` – List pending invitations (org scope)
- `list-product-members` – List product members
- `invite-member` – Invite a new member
- `update-member-permissions` – Update a member's permissions
- `delete-organization-member` – Remove an org member
- `delete-product-member` – Remove a product member
- `delete-invitation` – Cancel an outstanding invitation

#### Permission Groups
- `list-permission-groups` – List permission groups
- `create-permission-group` – Create a permission group
- `get-permission-group` – Get permission group details
- `update-permission-group` – Update a permission group
- `delete-permission-group` – Delete a permission group

### General

#### Products
- `list-products`
- `get-product`
- `update-product`
- `delete-product`
- `get-product-preferences`
- `update-product-preferences`
- `create-product`

#### Configs
- `list-configs`
- `create-config`
- `get-config`
- `update-config`
- `delete-config`

#### Environments
- `list-environments`
- `create-environment`
- `get-environment`
- `update-environment`
- `delete-environment`

#### Segments
- `list-segments`
- `create-segment`
- `get-segment`
- `update-segment`
- `delete-segment`

#### SDK Keys
- `get-sdk-keys` – Fetch SDK keys for a config/environment pair

#### Webhooks
- `list-webhooks`
- `get-webhook`
- `replace-webhook`
- `update-webhook`
- `delete-webhook`
- `get-webhook-signing-keys`
- `create-webhook`

#### Integrations
- `list-integrations`
- `create-integration`
- `get-integration`
- `update-integration`
- `delete-integration`

#### Code References
- `get-code-references`

### Diagnostics

#### Audit Logs
- `list-auditlogs` – Product audit logs
- `list-organization-auditlogs` – Organization audit logs

#### Zombie (Stale) Flags
- `list-staleflags` – Stale feature flags report

### Feature Flag Metadata

#### Feature Flags & Settings
- `list-settings`
- `create-setting`
- `get-setting`
- `replace-setting`
- `update-setting`
- `delete-setting`

#### Tags
- `list-tags`
- `create-tag`
- `list-settings-by-tag`
- `get-tag`
- `update-tag`
- `delete-tag`

### Feature Flag & Setting Values (v1 & v2)
- `get-setting-value`
- `update-setting-value`
- `replace-setting-value`
- `get-setting-values`
- `post-setting-values`
- V2 variants: each tool above has a `*-v2` counterpart for Config V2

## API Rate Limits

The Public Management API enforces rate limits. The MCP server surfaces limit exceed errors directly so clients can adapt (e.g. backing off or summarizing batched operations). See [Subscription Plan Limits](/subscription-plan-limits) for general guidance.

## Security Note

Credentials grant management rights. Store them securely, do not embed them in shared config, and rotate periodically. Avoid using the MCP server from untrusted or multi‑tenant environments.

## See Also

- [Management API Reference](/api/reference/configcat-public-management-api/)
- [SDK Reference](/sdk-reference/overview/)
- [Proxy Overview](/advanced/proxy/proxy-overview/)
- [Zombie (Stale) Flags](/zombie-flags)

---
Need another operation exposed? Let us know via the ConfigCat support channels.

