<p>
  ConfigCat's{' '}<a href={props.linkUrl} target={props.linkTarget}>{props.linkText}</a>{' '}has the ability to scan your source code for feature flag and setting usages
  and upload the found code references to ConfigCat.
</p>

This feature makes the elimination of the technical debt easier, as it can show which repositories reference your feature flags and settings in one centralized place on your <a target="_blank" href="https://app.configcat.com">Dashboard</a>.
